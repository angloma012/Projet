import { NullTemplateVisitor } from '@angular/compiler';

export interface technologie {
    id: number | null;
    technoname: string;
    caterne: string;
    date: string;
    category: string;
    exampleFormControlFile1: string;
    details: string;
}

export interface technologie {
    id: number | null;
    pub: string;
}